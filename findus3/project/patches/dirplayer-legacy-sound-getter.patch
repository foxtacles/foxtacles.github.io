diff --git a/vm-rust/src/player/bytecode/get_set.rs b/vm-rust/src/player/bytecode/get_set.rs
index f811822..cab3351 100644
--- a/vm-rust/src/player/bytecode/get_set.rs
+++ b/vm-rust/src/player/bytecode/get_set.rs
@@ -937,6 +937,20 @@ impl GetSetBytecodeHandler {
                 )?;
 
                 Ok(player.alloc_datum(Datum::String(last_chunk)))
+            } else if prop_type == 0x04 {
+                // Legacy `the <property> of sound N` uses the same channel state
+                // as sound(N).<property> and the corresponding kOpSet opcode.
+                let prop_name = get_sound_prop_name(prop_id as u16);
+                let channel_num_ref = {
+                    let scope = player.scopes.get_mut(ctx.scope_ref).unwrap();
+                    scope.stack.pop().unwrap()
+                };
+                let channel_num = player.get_datum(&channel_num_ref).int_value()?;
+                let sound_channel_datum = player.alloc_datum(Datum::SoundChannel(channel_num as u16));
+                let result = SoundChannelDatumHandlers::get_prop(
+                    player, &sound_channel_datum, Symbol::builtin(prop_name),
+                )?;
+                Ok(player.alloc_datum(result))
             } else if prop_type == 0x06 {
                 // sprite prop
                 let prop_name = sprite_prop_names().get(&(prop_id as u16));
